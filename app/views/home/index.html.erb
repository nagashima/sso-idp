<div class="container mx-auto px-4 py-8">
  <div class="max-w-md mx-auto bg-white rounded-lg shadow-md p-6">
    <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">
      SSO Identity Provider
    </h1>

    <% if logged_in? %>
      <div data-testid="logged-in-user-info" class="bg-green-50 border border-green-200 rounded-lg p-4 mb-4">
        <p class="text-green-800">
          <strong>ログイン中:</strong> <%= current_user.full_name %> (<%= current_user.email %>)
        </p>
      </div>

      <div class="mb-6">
        <h2 class="text-lg font-semibold text-gray-700 mb-3">利用可能なサービス</h2>
        <div class="space-y-2">
          <% if @relying_parties.present? %>
            <% @relying_parties.each do |rp| %>
              <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg border border-gray-200">
                <div class="flex-1">
                  <h3 class="font-medium text-gray-800"><%= rp.name %></h3>
                  <p class="text-sm text-gray-600"><%= rp.domain %></p>
                </div>
                <div class="flex items-center gap-3">
                  <% if @user_rp_ids.include?(rp.id) %>
                    <span class="px-3 py-1 bg-green-100 text-green-800 text-sm font-medium rounded">利用中</span>
                  <% else %>
                    <span class="px-3 py-1 bg-gray-100 text-gray-600 text-sm font-medium rounded">未使用</span>
                  <% end %>
                  <%= link_to 'サインイン', rp.sign_in_url,
                      target: '_blank',
                      rel: 'noopener noreferrer',
                      class: 'px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white text-sm font-semibold rounded' %>
                </div>
              </div>
            <% end %>
          <% else %>
            <p class="text-gray-500 text-sm">利用可能なサービスはありません</p>
          <% end %>
        </div>
      </div>

      <div class="flex gap-2">
        <%= button_to 'ログアウト', users_sign_out_path,
            method: :delete,
            form: { data: { testid: 'logout-button' } },
            class: 'flex-1 bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded' %>
      </div>

    <% else %>
      <% if defined?(@error_message) && @error_message %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-4">
          <p class="text-red-800">
            <%= @error_message %>
          </p>
        </div>
      <% end %>

      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-4">
        <p class="text-blue-800">
          アカウントをお持ちの方はログイン、新規の方は会員登録をしてください。
        </p>
      </div>

      <div class="space-y-3">
        <%= link_to 'ログイン', users_sign_in_path,
            class: 'w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded text-center block' %>

        <%= link_to '新規会員登録', users_sign_up_path,
            class: 'w-full bg-green-500 hover:bg-green-600 text-white font-semibold py-2 px-4 rounded text-center block' %>
      </div>
    <% end %>
  </div>
</div>